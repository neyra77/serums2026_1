#!/usr/bin/env python3
"""
filter_medicina.py
------------------
Filters medicina_remunerado_plazas.csv and medicina_equivalente_plazas.csv
to only MEDICINA rows.

Usage:
    python filter_medicina.py remunerado.csv equivalente.csv
    python filter_medicina.py *.csv
"""

import sys
import unicodedata
from pathlib import Path
import pandas as pd


def strip_diacritics(s: str) -> str:
    return "".join(
        c for c in unicodedata.normalize("NFD", s)
        if unicodedata.category(c) != "Mn"
    )

def is_medicina(val) -> bool:
    return strip_diacritics(str(val or "").strip().upper()) == "MEDICINA"


def filter_csv(path: Path) -> None:
    df = pd.read_csv(path, dtype=str).fillna("")

    prof_col = next(
        (c for c in df.columns if strip_diacritics(c.upper()).startswith("PROFES")),
        None
    )
    if prof_col is None:
        print(f"ADVERTENCIA: No se encontró columna PROFESION en {path.name} — omitiendo")
        return

    before = len(df)
    df = df[df[prof_col].apply(is_medicina)]
    after = len(df)

    out = path.parent / f"medicina_{path.name}"
    df.to_csv(out, index=False, encoding="utf-8")
    print(f"  {path.name}: {before} filas → {after} MEDICINA → {out.name}")


def main():
    if len(sys.argv) < 2:
        print("Uso: python filter_medicina.py archivo1.csv [archivo2.csv ...]")
        sys.exit(1)

    for arg in sys.argv[1:]:
        path = Path(arg)
        if not path.exists():
            print(f"ADVERTENCIA: No encontrado, omitiendo: {path}")
            continue
        if path.suffix.lower() != ".csv":
            print(f"ADVERTENCIA: No es CSV, omitiendo: {path}")
            continue
        try:
            filter_csv(path)
        except Exception as e:
            print(f"ERROR en {path.name}: {e}")


if __name__ == "__main__":
    main()
